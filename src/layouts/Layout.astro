---
import Header from "../components/Header.astro";
import "../styles/global.css";

---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Simão Santos - Software Engineer</title>
	</head>
	<body>
		<div class="relative h-full w-full bg-slate-950 overflow-x-hidden">
			<div class="max-[768px]:hidden absolute bottom-0 left-[-20%] right-0 top-[-10%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div>
			<div class="max-[768px]:hidden absolute bottom-0 right-[-20%] top-[-10%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div>
			
			<div class="max-[768px]:hidden absolute bottom-0 right-[-0%] top-[150%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div>
			<div class="max-[768px]:hidden absolute bottom-0 left-[-5%] top-[275%] h-[500px] w-[500px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.15),rgba(255,255,255,0))]"></div>

			<Header />
			<slot />	
		</div>
	</body>
</html>

<style>
	html{
		scroll-behavior: smooth !important; 
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		display: flex;
		flex-direction: column;
	}

	/* @media (prefers-reduced-motion: reduce) {
		html {
			scroll-behavior: auto;
		}
	} */
</style>
